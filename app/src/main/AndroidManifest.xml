<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:name=".AppApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".decoretorpatten.DecoratorActivity"></activity>
        <activity android:name=".decoretorpatten.DecoratorPatternActivity" />
        <activity android:name=".observerpattern.ObserverActivity" />
        <activity android:name=".commandpattern.CommandActivity" />
        <activity android:name=".stragetypattern.StrategyActivity" />
        <activity
            android:name=".factorypattern.FactoryActivity"
            android:label="@string/title_activity_factory"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".proxypattern.ProxyLoginActivity" />
        <activity android:name=".main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>